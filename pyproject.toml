[project]
name = "esp32-mcp"
version = "0.1.0"
description = "MCP server for controlling ESP32 with MicroPython via serial REPL"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.6.0",
    "pyserial>=3.5",
    "pydantic>=2.0",
]
